#include <stdio.h>
/**
 * main - entry point for task 5 program.
 *
 * Return: zero if no errors, return non zero if errors
 */
int main(void)
{
	print("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
